# DATABASE_URL: leave commented out to use the default SQLite file at
# rmas/rmas.db. Only uncomment/set this if you want the database somewhere
# else, or move back to PostgreSQL later (CLAUDE.md section 4).
# DATABASE_URL=
APP_NAME=Royal Metal Allocation System
APP_VERSION=5H
APP_TIMEZONE=Asia/Kolkata

# Long random value in any real environment — never use the code default.
SESSION_SECRET_KEY=
